import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
  variable: "--font-poppins",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Acknowledge | Landing Page",
  description: "An agile, unofficial school cyber security team of pentesting specialists from an academic background, securing networks, web apps, and system infrastructures.",
  keywords: ["cyber security", "penetration testing", "ethical hacking", "network pentesting", "web app security", "linux system audits", "sysadmin"],
  authors: [{ name: "ACKNOWLEDGE" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${poppins.variable}`}>
      <head>
        <link rel="icon" href="/favicon.ico" sizes="any" />
      </head>
      <body>
        {children}
      </body>
    </html>
  );
}
